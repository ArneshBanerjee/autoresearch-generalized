[project]
name = "lm-pretraining"
version = "0.1.0"
description = "GPT pretraining on ClimbMix for autoresearch"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0",
    "requests>=2.28",
    "pyarrow>=14.0",
    "rustbpe>=0.1",
    "tiktoken>=0.5",
]
